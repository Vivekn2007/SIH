import type { Telemetry } from '../types'
type Filter='open'|'smart'|'both'
export function Footer({ telemetry, filter, setFilter }: { telemetry:Telemetry|null;filter:Filter;setFilter:(f:Filter)=>void }) {return <footer><span><b>Team Perceptrons · SIH26055</b></span><span>Simulation Time: <b>{telemetry?.sim_time??'—'}</b></span><span>Current Step: <b>{telemetry?`${telemetry.step.toLocaleString()} / ${telemetry.total_steps.toLocaleString()}`:'—'}</b></span><span>Scenario: <b>RF ES Evaluation</b></span><span>CSV: <b>{telemetry?`${telemetry.data_source.name} — ${telemetry.data_source.mode}`:'—'}</b></span><span className="strategy">Scan Strategy: <div className="segmented">{([['open','Open-Loop'],['both','Both'],['smart','Smart Scan RL']] as const).map(([key,label])=><button className={filter===key?'active':''} onClick={()=>setFilter(key)} key={key}>{label}</button>)}</div></span><span>SINR <b>{telemetry?.sinr_db??'—'} dB</b></span><span className="system">● SYSTEM NOMINAL</span></footer>}
