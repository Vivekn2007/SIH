import type { Telemetry } from '../types'
type Filter='open'|'smart'|'both'
export function Footer({ telemetry, filter, setFilter }: { telemetry:Telemetry|null;filter:Filter;setFilter:(f:Filter)=>void }) {return <footer><span className="strategy">Scan Strategy: <div className="segmented">{([['open','Open-Loop'],['both','Both'],['smart','Smart Scan RL']] as const).map(([key,label])=><button className={filter===key?'active':''} onClick={()=>setFilter(key)} key={key}>{label}</button>)}</div></span><span>SINR <b>{telemetry?.sinr_db??'—'} dB</b></span><span className="system">{telemetry ? '● TELEMETRY ACTIVE' : '○ AWAITING TELEMETRY'}</span></footer>}
